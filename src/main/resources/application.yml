#file: noinspection SpringBootApplicationYaml
spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://dpg-d003du2li9vc739gqh4g-a/course_bd_lkem # Додайте власний хост
    username: user # Додайте власний логін
    password: F4kUYVbggOUnX9pZNiOrLUhLjN0J20Jo # Додайте власний пароль
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres

# Додано налаштування сервера
server:
  port: 8080
  servlet:
    context-path: /

  # Додано налаштування для Thymeleaf (якщо використовується)
  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
    cache: false